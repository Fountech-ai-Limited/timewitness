# One receipt from a GitHub-hosted runner, 2026-09-14

`receipt.hex` is the receipt asset of release build-34810453918 of the public repository
Fountech-ai-Limited/timewitness-install-check, produced on 2026-09-14 by the Action at v0 at sixteen
polling rounds. It is the artefact behind the 287.147 ms the register at `scripts/figures.json`
quotes as the widest a runner has reached, and it is committed here so that figure is held to bytes
in this repository rather than to a release on another host. The bytes are the release's
`widget.tar.gz.receipt` asset, written as hex because the tree holds one binary file and holds it on
purpose; decoded, their sha256 is
`f99afeafff88b7f45a79a421c55e8c432b6abd49da3dcf94edbaebfea2f2fcb2`.

`timewitness verify` accepts it with no account: 287.147 ms wide, nine sources behind six operators
and nine kept, three attestations carried and checked.
