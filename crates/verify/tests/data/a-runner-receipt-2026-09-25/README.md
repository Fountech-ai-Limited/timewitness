# One receipt from a GitHub-hosted runner, 2026-09-25

`receipt.hex` is the `widget.tar.gz.receipt` asset of release stamped-with-v0.4 of the public
repository Fountech-ai-Limited/timewitness-install-check, as run 36131291725 of that repository's
install check wrote it at 11:48 UTC on 2026-09-25: the Action at v0.4, installed in one line, at
sixteen polling rounds. That release is replaced on every run, so the bytes are committed here to
hold the figure to something that stays put. Written as hex for the same reason as the receipt of
2026-09-14 beside it; decoded, their sha256 is
`45985e5f937d8c5529b5f8831d7f410b4b52f58b0e0518c7190de315ba1f9ddb`.

`timewitness verify` at `main` accepts it with no account: 222.628 ms wide, nine sources behind six
operators and nine kept, three attestations carried and checked. It is the width the Action's own
words lead with from 2026-09-25, because the 211.3 ms they led with before rests on a receipt taken
before this product carried operators, which `verify` now refuses on the operator floor.
