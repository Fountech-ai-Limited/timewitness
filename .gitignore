/target
**/*.rs.bk
*.pdb

# The verifier page as one file, which is an output. `scripts/build-verifier-page.sh` makes it from
# the source beside it, and a release is where a stranger gets a copy.
/verifier-page/verifier.html
